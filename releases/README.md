# ICUdata releases

One folder per ICUdata release, holding the artifacts that describe that release.

## Layout

```
releases/
├── README.md                     # this page
└── v<MAJOR>.<MINOR>.<PATCH>-<YYYY-MM-DD>/
    ├── CHANGELOG.md              # what changed vs. the previous release
    └── dictionary.csv            # full concept dictionary as of this release
```

Each release folder is named with its version and release date, e.g.
`v0.2.0-2026-06-29/`.

## What's in a release folder

- **`CHANGELOG.md`** — auto-generated by diffing this release against the
  previous one. Sections (only those with content appear): *Hospitals Added*,
  *Tables Added*, *Schema Changes*, *Concept Changes*, *Parameter Remappings*.
- **`dictionary.csv`** — the full concept dictionary as of this release: every
  concept with its statistics (row counts, per-hospital patient coverage, value
  distributions).

## Versioning

Release folders use `v<MAJOR>.<MINOR>.<PATCH>`. The largest change in a release
determines the bump:

| Change in the release | Bump |
|-----------------------|------|
| Hospital added/removed, table added/removed, column type changed | minor (`0.x.0`) |
| Column added/removed, row-count change, concept added/removed, parameter remap | patch (`0.0.x`) |
| Nothing | no change |

## Releases

| Version | Date | Notes |
|---------|------|-------|
| [v0.2.0](v0.2.0-2026-06-29/) | 2026-06-29 | _Pending_ |
| [v0.1.0](v0.1.0-2026-03-04/) | 2026-03-04 | Baseline dictionary (no changelog) |
